{
  "name": "hamo-monorepo",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "playground"],
  "scripts": {
    "build": "bun --filter hamo build",
    "dev": "bun --filter hamo dev",
    "typecheck": "bun --filter hamo typecheck",
    "lint": "bun --filter hamo lint",
    "format": "bun --filter hamo format",
    "test": "bun --filter hamo test"
  }
}
