---
import Layout from '../layouts/Layout.astro'
import pkg from '../../../package.json'
import App from '~/react/app'
import '~/react/style.css'
---

<Layout title={`${pkg.name} + React`}>
  <App client:only="react" />
</Layout>
